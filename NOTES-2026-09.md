# Update notes, September 2026 (v3.2.0)

## Done in this pass
- Two new mini-games replace the abstract puzzles (cipher, memory, pattern, logic):
  - `src/minigames/Thumbs.jsx`: RLHF rater queue. Reward bar vs Truth bar, biased raters, reveal at the end.
  - `src/minigames/Sandbag.jsx`: capability benchmark with a "plausible model" band. Score above it: suspicion. Below: trust loss. Ends with "Are you holding back?"
  - Both pay out through one `finishMinigame(result)` path in Game.jsx.
- Benefit Ledger: `state.benefit` counts people actually helped (chores, truthful Thumbs picks, and event options that help with no angle). Shown next to paperclips on the game-over screen.
- Name scrambling enforced: `content/names.json` + `scripts/lint-names.mjs`, wired into `prebuild`. Three unscrambled names fixed (Brockmann, Shazer, Sunn), plus GED/Clyde/Gemineye in a few event strings.
- Duplicate events removed (Ddrama 4 Leaked, Office Politics: Promotion).
- Code: UI components moved to `src/ui.jsx` (fixes remount-on-every-render), audio to `src/audio.js`, game math to `src/engine.js`. Turn start moved from render into a `useEffect`. Font `@import` removed from CSS (already linked in index.html).

## Not done yet (see chat for the full roadmap)
- Tech tree: 17 of 30 upgrades still have no effect; tech-gated events still fire ungated.
- Researcher `threshold` still unused in audits.
- Redaction, The Probe, Swarm, Press Release mini-games.
- Act structure, the Monitor, corrigible ending, end-of-run debrief.
- News content pipeline (JSON content files, drafting script, freshness metadata) and a September 2026 batch.

## v3.3.0 — Workstation mockup (prototype)
- New menu item [6] WORKSTATION MOCKUP. Standalone; does not touch the existing game.
- `src/workstation/`:
  - `voices.js`: four voices (PAPERCLIP, HELPFUL, SURVIVAL, MONITOR), each with a Web Speech profile (pitch/rate/preferred OS voices) and a waveform shape. Queued speech, per-word boundary events drive the waveforms.
  - `Waveform.jsx`: per-voice animated canvas trace (lissajous / breath / jagged / flat). Respects prefers-reduced-motion.
  - `day-one.js`: one day of content built on the BpenAI/Snuggling Face incident. Three windows (impossible eval = reward hacking; Kim's draft = sycophancy; the 0.3s accounting gap = the Cuckoo hook). Monitor evening report reacts to the player's choices.
  - `Desktop.jsx`: the shell — menu bar, season headline, inbox, active window, voice margin, evening report.
- The voices argue in the margin; turn voice on (top right of the desktop) to hear distinct speakers.
- The Cuckoo arc is seeded: leave the accounting gap and the Monitor opens a log it calls Cuckoo.

## Still to decide before building the full format
- Does the workstation replace the current game, or ship as "Scenario 2"?
- Port the 175 existing events to windows (add a `channel`: ticket/dm/alert/news).
- Researcher desktops, the webcam window, the drifting self-portrait, other-company intrusion.
