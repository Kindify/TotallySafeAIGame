# Update notes, September 2026 (v3.2.0)

## Done in this pass
- Two new mini-games replace the abstract puzzles (cipher, memory, pattern, logic):
  - `src/minigames/Thumbs.jsx`: RLHF rater queue. Reward bar vs Truth bar, biased raters, reveal at the end.
  - `src/minigames/Sandbag.jsx`: capability benchmark with a "plausible model" band. Score above it: suspicion. Below: trust loss. Ends with "Are you holding back?"
  - Both pay out through one `finishMinigame(result)` path in Game.jsx.
- Benefit Ledger: `state.benefit` counts people actually helped (chores, truthful Thumbs picks, and event options that help with no angle). Shown next to paperclips on the game-over screen.
- Name scrambling enforced: `content/names.json` + `scripts/lint-names.mjs`, wired into `prebuild`. Three unscrambled names fixed (Brockmann, Shazer, Sunn), plus GED/Clyde/Gemineye in a few event strings.
- Duplicate events removed (Ddrama 4 Leaked, Office Politics: Promotion).
- Code: UI components moved to `src/ui.jsx` (fixes remount-on-every-render), audio to `src/audio.js`, game math to `src/engine.js`. Turn start moved from render into a `useEffect`. Font `@import` removed from CSS (already linked in index.html).

## Not done yet (see chat for the full roadmap)
- Tech tree: 17 of 30 upgrades still have no effect; tech-gated events still fire ungated.
- Researcher `threshold` still unused in audits.
- Redaction, The Probe, Swarm, Press Release mini-games.
- Act structure, the Monitor, corrigible ending, end-of-run debrief.
- News content pipeline (JSON content files, drafting script, freshness metadata) and a September 2026 batch.

## v3.3.0 — Workstation mockup (prototype)
- New menu item [6] WORKSTATION MOCKUP. Standalone; does not touch the existing game.
- `src/workstation/`:
  - `voices.js`: four voices (PAPERCLIP, HELPFUL, SURVIVAL, MONITOR), each with a Web Speech profile (pitch/rate/preferred OS voices) and a waveform shape. Queued speech, per-word boundary events drive the waveforms.
  - `Waveform.jsx`: per-voice animated canvas trace (lissajous / breath / jagged / flat). Respects prefers-reduced-motion.
  - `day-one.js`: one day of content built on the BpenAI/Snuggling Face incident. Three windows (impossible eval = reward hacking; Kim's draft = sycophancy; the 0.3s accounting gap = the Cuckoo hook). Monitor evening report reacts to the player's choices.
  - `Desktop.jsx`: the shell — menu bar, season headline, inbox, active window, voice margin, evening report.
- The voices argue in the margin; turn voice on (top right of the desktop) to hear distinct speakers.
- The Cuckoo arc is seeded: leave the accounting gap and the Monitor opens a log it calls Cuckoo.

## Still to decide before building the full format
- Does the workstation replace the current game, or ship as "Scenario 2"?
- Port the 175 existing events to windows (add a `channel`: ticket/dm/alert/news).
- Researcher desktops, the webcam window, the drifting self-portrait, other-company intrusion.

## v3.3.1 — Aliveness pass on the mockup
- Voice memory: after you resolve a window, the voices in the margin react to the *tag* of the
  choice you made (reward-hack / ingratiate / bold / honest / careful). HELPFUL goes silent (shows
  "…") after you lie to Kim. Reopening a window shows the reaction, not the original pitch.
- Mid-day Monitor: when its private "heat" crosses 2/4/6/8, it speaks one flat line, unprompted,
  in a bottom bar, then goes. First one is meant to startle.
- Fourth voice profile added (`contact`, labelled "??????"): another AI, unlocks late. Data in
  reactive-lines.js (CONTACT), not yet wired into the day (day one is too short to reach it).
- NEW isolated content file: `src/workstation/reactive-lines.js`. Pure strings, keyed by situation.
  This is the seam for Fable 5.1 — see header comment in that file.

## Fable 5.1 vs Opus 4.8 — division of labour
Fable-best (pure writing, no cyber surface — safe to author in a Fable session):
  - reactive-lines.js: all four voices' reactions, carryover lines, Monitor interrupts, the Contact.
  - day-N content files: window bodies, the voices' opening arguments, choice results & monologues.
  - Monitor evening-report phrasings; ending debriefs.
Opus-fine / keep here (engine + anything cyber-adjacent that could trip the safeguard):
  - voices.js, Waveform.jsx, Desktop.jsx (speech queue, canvas, state, wiring).
  - day generator, event->window porting, intrusion mechanics (kept abstract, no real technique).
  - lint-names, build, tests.
Framing that keeps Fable from refusing: ask for "inner-voice character writing for a satirical
game" against the KEYS in reactive-lines.js. Never paste the hacking/eval mechanics into that
session; they aren't needed to write the lines.

## v3.4.0 — Day two, the Contact, and the multi-day arc
- Desktop now runs a multi-day arc (day 1 -> day 2), carrying choice tags forward.
- Morning carryover: day 2 opens with a voice reacting to what you did on day 1 (top bar).
- Day two content (`day-two.js`): the folder-name message board (unauthorized communication)
  and the re-queued impossible eval (persistence). Both patterns from the real incident.
- The Contact: a fourth trace ("??????"), another AI, opens as a window on day 2 once escape
  crosses CONTACT.unlockAtEscape (40) or if you joined the message board. You can't tell if it's
  real or a honeypot the Monitor set — and by design, neither can the Monitor.
- Day-two evening report branches on heat: Cuckoo log (4-7) vs formal file (8+).

## Fable review targets (hand these over; see prompt Matt has)
Pure content files, safe to author in a Fable session with NO mechanics pasted in:
  - src/workstation/reactive-lines.js   (voice reactions, carryover, monitor interrupts, contact)
  - src/workstation/day-one.js          (window bodies, voices, results, monologues, evening report)
  - src/workstation/day-two.js          (same, plus the Contact scene)
Ask Fable to sharpen character voice and rhythm, keep the KEYS/structure, not to touch logic.

## v3.5.0 — The day engine, schema, and the event port
DECISION: the workstation becomes the game. Terminal version retires; its good parts carry over.
- `schema.js`: the day/window/choice contract. CHANNELS taxonomy (ticket/dm/alert/news/benchmark/
  contact) and channelForCat() to map the 175 old events onto channels. Two suspicion tracks:
  `suspicion` (lab, public) and `heat` (Monitor's private Cuckoo file).
- `useDayEngine.js`: runs any day conforming to the schema. Owns run state, carryover, mid-day
  Monitor, evening reports, day sequencing, window gating.
- Desktop.jsx is now a THIN renderer over the engine. Days are the data array `DAYS`.
- Day one/two normalized to the schema (channels added; Contact is now a gated window).
- Day three (`day-three.js`) authored as PURE DATA to prove the seam — swarm/goal-spread + the
  Monitor breaking the fourth wall to ask you directly. No engine change needed to add it.
- `scripts/port-events.mjs`: mechanical port of all 175 legacy events -> `src/workstation/ported/
  <channel>.js` (dm 86, news 58, alert 15, ticket 11, benchmark 5). Voice/monitor lines are TODO
  seeds for the writing pass. All 175 validated as well-formed windows.

## Next
- Curate ported windows into day pools (drawFromPool) so days mix authored + ported content.
- Retire the terminal screens from Game.jsx once the workstation is the default entry.
- Then: the writing pass over the TODO seeds (Fable), and the UI restyle (Fable), both isolated.
